---
description: Draft a Kody capability
argument-hint: <what should it do>
---

Draft a Kody capability that does the following: $ARGUMENTS.

Output a folder proposal for `.kody/capabilities/<slug>/` containing
`instructions.md`, `skills/`, and `tools/`. The instructions explain the one
input value, the work, and the one output value in plain language.
