{
  "input": {
    "name": "request",
    "schema": {
      "type": "object",
      "properties": {
        "issue": {
          "type": "integer",
          "description": "GitHub issue number containing the focused model creation request."
        },
        "dry_run": {
          "type": "boolean",
          "description": "Validate the generated workflow without creating a state-repo branch or pull request."
        }
      },
      "required": [
        "issue"
      ],
      "additionalProperties": false
    }
  },
  "output": {
    "name": "result",
    "schema": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "WORKFLOW_CREATOR_COMPLETED",
            "WORKFLOW_CREATOR_FAILED",
            "AGENT_NOT_RUN"
          ]
        },
        "reason": {
          "type": "string"
        },
        "summary": {
          "type": "string"
        },
        "data": {
          "type": "object",
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    }
  }
}
