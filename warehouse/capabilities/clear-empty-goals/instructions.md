# Instructions

Use the `clear-empty-goals` skill.

Remove only goals proven to have no tasks. Report ambiguous records without
changing them.

Return `DONE` with a short `PR_SUMMARY`, or `FAILED: <reason>`.


---

# Clear Empty Goals

Find goals that contain no tasks. Remove only goals that are proven empty, and
report anything ambiguous without changing it.
