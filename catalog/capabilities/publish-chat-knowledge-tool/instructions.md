# Publish Chat knowledge tool

Publish the validated output of `build-chat-knowledge-graph` as repository-scoped
data for Kody's trusted `knowledge_graph_search` handler.

This capability only publishes data. It never generates knowledge, enables the
tool, or executes uploaded code.
