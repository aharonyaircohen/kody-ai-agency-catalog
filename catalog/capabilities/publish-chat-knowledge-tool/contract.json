{
  "execution": "script",
  "input": {
    "type": "object",
    "properties": {
      "schemaVersion": { "const": 1 },
      "kind": { "const": "chat-knowledge-graph" },
      "status": { "enum": ["built", "blocked"] },
      "summary": { "type": "string" },
      "graph": {
        "type": "object",
        "properties": {
          "nodes": { "type": "array" },
          "edges": { "type": "array" }
        },
        "required": ["nodes", "edges"]
      },
      "sources": { "type": "array" },
      "coverage": { "type": "array" },
      "gaps": { "type": "array" }
    },
    "required": [
      "schemaVersion",
      "kind",
      "status",
      "summary",
      "graph",
      "sources",
      "coverage",
      "gaps"
    ]
  },
  "output": {
    "type": "object",
    "properties": {
      "status": { "enum": ["published", "blocked"] },
      "summary": { "type": "string" },
      "toolId": { "const": "company-understanding" },
      "enabled": { "type": "boolean" }
    },
    "required": ["status", "summary", "toolId", "enabled"],
    "additionalProperties": false
  }
}
