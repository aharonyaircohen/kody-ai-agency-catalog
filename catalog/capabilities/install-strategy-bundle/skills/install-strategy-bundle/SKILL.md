---
name: install-strategy-bundle
description: Write the trusted Store installation bundle before Strategy reasoning.
---

# Install Strategy Bundle

Run the bundled deterministic installer. It merges the Store configuration
patch without removing repository values and writes only the supplied
Store-owned loop files.
