{
  "execution": "agent",
  "requirements": {
    "browser": true,
    "qaCredentials": true,
    "githubTestToken": true
  },
  "input": {
    "type": "object",
    "properties": {
      "pr": {
        "type": "integer",
        "minimum": 1
      },
      "previewUrl": {
        "type": "string",
        "format": "uri"
      }
    },
    "required": ["pr"],
    "additionalProperties": true
  },
  "output": {
    "type": "object",
    "properties": {
      "status": {
        "enum": ["pass", "fix", "blocked"]
      },
      "feedback": {
        "type": "string"
      },
      "summary": {
        "type": "string"
      }
    },
    "required": ["status", "feedback", "summary"],
    "additionalProperties": false
  }
}
