# Prepare CI Repair

Create or reuse the one GitHub issue that owns a default-branch CI repair.
Reuse a linked open repair pull request when one exists. This deterministic
Capability owns repair-task state only; it does not inspect CI or change code.
