{
  "execution": "script",
  "timeoutMs": 2000000,
  "input": {
    "type": "object",
    "properties": {
      "pr": { "type": "integer" }
    },
    "required": ["pr"]
  },
  "output": {
    "type": "object",
    "properties": {
      "version": { "const": 1 },
      "status": { "enum": ["pass", "fail", "blocked", "changed", "noop"] },
      "summary": { "type": "string" },
      "evidence": {
        "type": "object",
        "properties": {
          "releaseValidated": { "const": true }
        },
        "required": ["releaseValidated"]
      },
      "facts": {
        "type": "object",
        "properties": {
          "validationPr": { "type": "integer" },
          "validationWorkflow": { "type": "string" },
          "validationHeadSha": { "type": "string" },
          "validationRun": { "type": "integer" },
          "validationRunUrl": { "type": "string" },
          "validationConclusion": { "const": "success" }
        },
        "required": [
          "validationPr",
          "validationWorkflow",
          "validationHeadSha",
          "validationRun",
          "validationRunUrl",
          "validationConclusion"
        ]
      }
    },
    "required": ["version", "status", "summary", "evidence", "facts"]
  }
}
