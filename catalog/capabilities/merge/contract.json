{
  "execution": "agent",
  "input": {
    "type": "object",
    "properties": {
      "pr": {
        "type": "integer",
        "minimum": 1
      }
    },
    "required": ["pr"],
    "additionalProperties": true
  },
  "output": {
    "type": "object",
    "properties": {
      "status": {
        "enum": ["merged", "blocked"]
      },
      "merged": {
        "type": "boolean"
      },
      "pr": {
        "type": "integer",
        "minimum": 1
      },
      "summary": {
        "type": "string"
      }
    },
    "required": ["status", "merged", "pr", "summary"],
    "additionalProperties": false
  }
}
