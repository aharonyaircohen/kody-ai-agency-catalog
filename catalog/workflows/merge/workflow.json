{
  "name": "Merge",
  "createdAt": "2026-08-08T00:00:00Z",
  "updatedAt": "2026-08-08T00:00:00Z",
  "inputSchema": {
    "type": "object",
    "properties": {
      "pr": {
        "type": "integer",
        "minimum": 1
      },
      "headSha": {
        "type": "string",
        "minLength": 7,
        "maxLength": 64
      },
      "verdict": {
        "enum": ["pass", "fix"]
      },
      "status": {
        "enum": ["pass", "fix", "blocked"]
      }
    },
    "required": ["pr"],
    "additionalProperties": false
  },
  "startAt": "merge",
  "steps": [
    {
      "id": "merge",
      "capability": "merge",
      "target": "pr"
    }
  ],
  "agent": "kody"
}
