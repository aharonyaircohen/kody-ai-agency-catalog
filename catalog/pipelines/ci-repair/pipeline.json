{
  "name": "CI Repair, Review & Merge",
  "runWithoutApproval": true,
  "createdAt": "2026-08-10T00:00:00Z",
  "updatedAt": "2026-08-11T00:00:00Z",
  "inputSchema": {
    "type": "object",
    "properties": {
      "pr": { "type": "integer", "minimum": 1 },
      "branch": { "type": "string", "minLength": 1, "maxLength": 255 },
      "ciRunId": { "type": "integer", "minimum": 1 },
      "headSha": { "type": "string", "minLength": 7, "maxLength": 64 }
    },
    "required": ["branch", "ciRunId", "headSha"],
    "additionalProperties": false
  },
  "steps": [
    { "id": "ci-repair", "workflow": "ci-repair" },
    { "id": "review-and-fix", "workflow": "review-fix" },
    { "id": "merge", "workflow": "merge" }
  ]
}
