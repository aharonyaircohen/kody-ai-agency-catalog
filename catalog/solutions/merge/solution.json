{
  "schemaVersion": 1,
  "id": "merge",
  "name": "Merge",
  "description": "Safely merge a pull request after its required checks pass",
  "entrypoints": [
    {
      "kind": "workflow",
      "id": "merge"
    }
  ]
}
